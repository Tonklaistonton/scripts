```bash
crave run --no-patch "rm -rf scripts && git clone https://github.com/Tonklaistonton/scripts.git --depth=1 && chmod u+x scripts/derpfest15.sh && ./scripts/derpfest15.sh "
```
If already sync derp
```bash
crave run --no-patch "rm -rf scripts && git clone https://github.com/Tonklaistonton/scripts.git --depth=1 && chmod u+x scripts/derpfest15resync.sh && ./scripts/derpfest15resync.sh "
```
For evolution x 
```bash
crave run --no-patch "rm -rf scripts && git clone https://github.com/Tonklaistonton/scripts.git && chmod u+x scripts/evolution/sync.sh && ./scripts/evolution/sync.sh "
```
